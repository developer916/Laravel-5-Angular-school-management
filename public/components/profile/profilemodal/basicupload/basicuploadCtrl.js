(function () {
    'use strict';

    angular
        .module('app.profile.profilemodal.basicupload')
        .controller('ProfileModalBasicuploadController', profileModalBasicuploadController);

    function profileModalBasicuploadController() {
alert('ProfileModalBasicuploadController');
        var vm = this;
        //vm.university = university;

        activate();

        function activate() {

        }
    }
})();